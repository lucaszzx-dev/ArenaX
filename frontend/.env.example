# URL da API (backend). Em desenvolvimento NAO e necessario: o Vite
# faz proxy de /api para http://localhost:3333 (veja vite.config.ts).
# Definir apenas em producao (painel da Vercel/Render), ex.:
# VITE_API_URL=https://backend-do-arenax.example.com/api
